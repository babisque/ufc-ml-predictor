version: '3.8'

services:
  ufc-bot:
    build: .
    container_name: ufc_oracle_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/Sao_Paulo 
    volumes:
      - ./data:/app/data
      - ./models:/app/models